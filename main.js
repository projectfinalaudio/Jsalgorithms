const submitForm = () => {
    var form = document.querySelector("#form");

    for (i = 0; i < form.length; i++) {
        form[i].style.display = "none";
    };

    var submiited = document.getElementById("form");
    var text = "";
    var i;

    for (i = 0; i < submitted.length; i++) {
        text += submiited[i].value + "<br>"
    };
};

function headerAnimation() {
    document.querySelector("#main-heading").addEventListener("click", );


};